<%# <%= form_for @appointment do |f| %> 

        <%# <%= f.label "Select Subject" %> 
        <%# <%= f.select :tutor_id, options_from_collection_for_select(Tutor.all, "id", "SME") %> 

        <%# <%= f.label :date %> 
        <%# <%= f.date_select :appointment_time, {order: [:month, :day, :year], prompt: { day: 'Select day', month: 'Select month', year: 'Select year' }, start_year: Date.today.year, end_year: Date.today.year + 5}, {required: true} %> 

<%# <%= f.submit yield(:button_text), class: "btn btn-primary" %> 
<%# <% end %> 


=begin
<li id="appointment-<%= appointment.id %>">
  <%= link_to gravatar_for(appointment.student, size: 50), appointment.student %>
  <span class="user"><%= link_to appointment.student.username, appointment.student %></span>
  <span class="content"><%= appointment.appointment_time %></span>


<% if current_student?(appointment.student) %>
      <%= link_to "delete", appointment, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>


</li>
=end

